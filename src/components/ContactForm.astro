<h1>Contact Us</h1>

<form action="https://formspree.io/f/mkgjnwqo" method="POST" class="contact-form">
    <label for="name">
        <input type="text" name="name" placeholder="Name" required>
    </label>

    <label for="email">
        <input type="email" name="email" placeholder="your@email.com" required>
    </label>

    <label for="message">
        <textarea name="message" placeholder="What services are you interested in?" required></textarea>
    </label>

    <button type="submit">Submit</button>
</form>

<style>
    h1 {
        text-align: center;
        margin-top: 70px;
        margin-bottom: 15px;
        color: #D9A695;
        font-family: 'Poppins', sans-serif;
    }

  /* Centering the form */
    .contact-form {
        display: flex;
        flex-direction: column;
        gap: 20px;
        width: 300px;
        margin: auto;
        padding: 24px;
        position: relative;
        font-family: 'Poppins', sans-serif;;
    }


  /* Styling inputs and textarea */
  .contact-form input,
  .contact-form textarea {
      width: 100%;
      border: none;
      border-bottom: 3px solid #D9A695;
      padding: 10px 5px;
      font-size: 16px;
      outline: none;
      background: transparent;
  }

  .contact-form textarea {
      height: 50px;
      resize: none;
  }

  /* Styling labels */
  .contact-form label {
      font-size: 14px;
      font-weight: bold;
      color: #333;
  }

  /* Styling the submit button */
  .contact-form button {
      align-self: flex-end;
      background-color: #D9A695;
      color: white;
      border: none;
      padding: 12px 24px;
      font-size: 16px;
      font-weight: bold;
      cursor: pointer;
      transition: background 0.3s ease;
      border-radius: 4px;
  }

  .contact-form button:hover {
      background-color: #b78577;
  }

</style>
